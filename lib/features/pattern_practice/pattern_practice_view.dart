import 'package:flutter/material.dart';

class PatternPracticeView extends StatelessWidget {
  const PatternPracticeView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Pattern Practice')),
      body: const Center(child: Text('Pattern Practice placeholder')),
    );
  }
}
